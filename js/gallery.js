// const picturesContainer = document.querySelector('.pictures');
// const links = Array.from(picturesContainer.children).filter(child =>
//   child.classList.contains('picture')
// );
// export { links };
